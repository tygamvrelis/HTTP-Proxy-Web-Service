# HTTP-Proxy-Web-Service

HTTP web server that runs on a specified hostname and port. Fulfills GET and POST requests by proxy, given a URL of the form http://HOSTNAME/.../ and data as key-value pairs
